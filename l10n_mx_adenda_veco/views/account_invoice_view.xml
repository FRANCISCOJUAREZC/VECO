<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_inherit_form" model="ir.ui.view">
            <field name="name">account.move.inherit.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form" />
            <field name="arch" type="xml">
                <xpath expr="//page[@name='other_info']" position="inside">
                    <group attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))]}">
                        <group string="Adenda OC">
                            <field name="veco_agregada"/>
                        </group>
                    </group>
                </xpath>
            </field>
    </record>

    <record id="model_action_addenda_oc" model="ir.actions.server">
        <field name="name">Adenda OC</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">action = records.action_add_addenda_oc()</field>
    </record>

</odoo>
